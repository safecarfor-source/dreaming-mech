# /전체보고

**이 명령은 무조건 종합 브리핑을 실행한다. 인자 없음. 해석 없음. 질문 없음. 즉시 실행.**

## 연결 정보
- REST API: http://127.0.0.1:27123
- API Key: Bearer 0b0350be06da430e66f15c94dd9946958173c0c0b85bb438a91023b36c3017e3
- 볼트 경로: /Users/shinjeayoun/Library/Mobile Documents/iCloud~md~obsidian/Documents/시작. 1/

## 1원칙: "한 것 / 안 한 것"

모든 데이터를 이 두 분류로 나눈다. 예외 없음.

---

## 데이터 수집 (가능한 한 병렬 실행)

### 소스 1: 옵시디언 볼트

**절대 규칙: 볼트의 모든 .md 파일을 하나도 빠짐없이 읽고, 하나도 빠짐없이 보고에 포함한다.**
- 파일을 생략하거나 요약하지 않는다
- .canvas 파일만 무시
- 파일이 50개든 100개든 전부 읽는다. 건너뛰기 금지.

실행 순서 (⚡ 속도 최적화):
1. REST API GET /vault/ → 루트 파일 목록 확인
2. REST API GET /vault/{폴더}/ → 각 하위 폴더 파일 목록 확인 (✅ 할일, 03-Actions, 04-AI-Research, 05-Content, 📋 정리됨, 📅 일정, 01-Memory, 📥 Inbox, 06-Templates)
   - ⚡ **🗄️ 완료/ 폴더는 읽지 않는다.** 파일 개수만 세서 "한 것" 건수에 반영.
3. **파일 내용 읽기는 Filesystem MCP `mcp__filesystem__read_multiple_files` 사용** — 한 번에 여러 파일을 묶어서 읽는다 (REST API 하나씩 호출보다 훨씬 빠름)
   - 볼트 경로: `/Users/shinjeayoun/Library/Mobile Documents/iCloud~md~obsidian/Documents/시작. 1/`
   - 예: `["/볼트경로/파일1.md", "/볼트경로/폴더/파일2.md", ...]` 형태로 경로 배열 전달
   - 10~15개씩 묶어서 병렬 호출하면 더 빠름
4. 하위 폴더 안에 또 폴더가 있으면 그 안도 스캔
5. REST API 폴더 목록 조회 실패 시에도 Filesystem MCP `mcp__filesystem__list_directory`로 폴백

### 소스 2: 구글 캘린더
- `gcal_list_events` (timeMin: 오늘 00:00, timeMax: 내일 23:59, timeZone: Asia/Seoul)

### 소스 3: 개발 프로젝트
- `tasks/todo.md` 읽기
- MEMORY.md "현재 진행 중" 섹션 참조

### 소스 4: Git 히스토리
- `git log --oneline -5`

### 소스 5: 서버 상태
- `curl -s -o /dev/null -w "%{http_code}" https://dreammechaniclab.com`
- `curl -s -o /dev/null -w "%{http_code}" https://dreammechaniclab.com/api/health`

### 소스 6: 📥 Inbox
- REST API GET Inbox/ → .md 파일 개수 (사용법.md 제외)

---

## 분류 원칙

### ✅ 한 것 (완료 — 앞으로 필요 없는 것)
판별: `[x]` 체크, `#완료` 태그, `completed:` frontmatter, `status: 완료`, 🗄️ 완료/ 폴더 파일

→ **개수만 표시.** 끝난 건 끝난 것.

⚠️ **"한 것"으로 분류하는 것은 대장님이 직접 "완료" 또는 "삭제" 명령했을 때만.**

**완료 처리 절차 (대장님 명령 시):**
1. `#완료` 태그 + `completed: YYYY-MM-DD` frontmatter 추가
2. `🗄️ 완료/` 폴더로 이동
3. 원본 삭제는 대장님이 직접

### ❌ 안 한 것 (앞으로 해야 할 모든 것)
위 "한 것"에 해당하지 않는 모든 파일/항목.

→ **소분류하여 상세 보고. 모든 파일이 반드시 어딘가에 등장해야 한다.**

| 소분류 | 포함 내용 |
|--------|-----------|
| 📅 일정 | 📅 일정/ 폴더 전체 + 캘린더 일정 |
| 🔴 긴급 | 오늘/내일 마감 |
| ✅ 할일 | ✅ 할일/ 폴더 전체 |
| 💻 개발 | tasks/todo.md 미완료, 진행중 프로젝트 |
| 🟡 프로젝트/사업 | 03-Actions/, ERP, SaaS 등 |
| 🎬 콘텐츠 | 유튜브 대본/촬영/기획, 05-Content/ |
| 📚 공부 | 04-AI-Research/ 전체 |
| 📋 정리/메모 | 📋 정리됨/, 📥 Inbox, 볼트 루트, 기타 |
| 🟢 개인 | 여행, 개인 메모 |

---

## 출력 규칙

⚠️ **모든 파일을 "제목 — 30자 내외 요약" 형식으로 한 줄씩 나열. 폴더 단위 요약 금지. 파일 하나하나 전부.**

⚠️ **검증: 보고 완료 후, 읽은 파일 총 개수 vs 보고서 등장 파일 개수 대조. 빠진 파일 있으면 추가.**

## 출력 형식

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🎖️ 대장님 전체보고 — {YYYY-MM-DD}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🌐 서버: 프론트 ✅/❌ | 백엔드 ✅/❌
📊 볼트: 총 {N}개 파일 스캔 완료

━━━ ✅ 한 것 (완료) ━━━
총 {N}건

━━━ ❌ 안 한 것 ━━━

📅 캘린더 일정
| 시간 | 일정 |
|------|------|
| {HH:MM} | {일정명} |

📅 일정/ ({N}개)
- {파일명} — {30자 요약}
- {파일명} — {30자 요약}

🔴 긴급 (오늘/내일)
- {파일명} — {30자 요약}

✅ 할일/ ({N}개)
- {파일명} — {30자 요약}
- {파일명} — {30자 요약}

💻 개발
- {todo.md 미완료 항목}
- {진행중 프로젝트}

🟡 03-Actions/ ({N}개)
- {파일명} — {30자 요약}

🎬 콘텐츠 ({N}개)
- {파일명} — {30자 요약}

📚 04-AI-Research/ ({N}개)
- {파일명} — {30자 요약}

📋 정리됨/ ({N}개)
- {파일명} — {30자 요약}

📁 볼트 루트 ({N}개)
- {파일명} — {30자 요약}

📥 Inbox: {N}개 정리 대기

🟢 개인
- {파일명} — {30자 요약}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
(해당 소분류에 항목 없으면 생략)

## 실패 처리
- 개별 소스 실패 시 해당 부분만 "⚠️ 수집 실패" 표시
- 나머지 정상 출력
